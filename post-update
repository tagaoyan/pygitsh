#!/bin/sh

if which fifoclient.py &> /dev/null; then
    echo "call deploy hook"
    timeout 10 fifoclient.py "$(git config deploy.name)" "$(git config deploy.key)" &
else
    echo "no work needed"
fi
